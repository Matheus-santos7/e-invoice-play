# Backend (alvo: Fastify + Prisma)

Este diretório receberá a API **Fastify** e o **Prisma** (`prisma/` pode ficar em `backend/prisma/` alinhado à regra `@01-db-dba`).

Não importe código de `frontend/`. Exponha contratos HTTP + Zod; o Next consome via fetch.
